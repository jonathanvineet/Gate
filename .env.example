# Frontend env (Vite)
# Default to Polygon mainnet (137)
VITE_STAKING_CHAIN_ID=137
# Example staking contract address (replace after deploy)
VITE_STAKING_CONTRACT=0x0000000000000000000000000000000000000000
# WMATIC token on Polygon mainnet
VITE_STAKING_TOKEN=0x0d500B1d8E8eF31E21C99d1Db9A6444d3ADf1270

# OKX DEX proxy base is configured in vite.config.ts if needed

# OKX DEX Aggregator credentials (dev proxy reads these; not exposed to client)
# Required: some deployments accept only x-api-key, others require full OK-ACCESS-* signing.
# Obtain from OKX developer portal. Do not commit real values.
# VITE_OKX_DEX_API_KEY=your_api_key
# VITE_OKX_DEX_API_SECRET=your_api_secret
# VITE_OKX_DEX_API_PASSPHRASE=your_api_passphrase
# VITE_OKX_DEX_API_KEY_HEADER=x-api-key
# Optional bearer-like token used by some gateways (usually not required for signed requests)
# VITE_OKX_DEX_ACCESS_TOKEN=
# Override target if needed (defaults to https://www.okx.com/api/v5/dex/aggregator)
# VITE_OKX_DEX_PROXY_TARGET=https://www.okx.com/api/v5/dex/aggregator
# Disable signing (only x-api-key) by setting to 'false'
# VITE_OKX_DEX_SIGN=true
